import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-sans",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700"],
});

const playfair = Playfair_Display({
  variable: "--font-display",
  subsets: ["latin"],
  weight: ["400", "500", "600", "700", "800"],
});

export const metadata: Metadata = {
  title: "Campari Group Africa — Digital Advertising & Influencer Guidance",
  description:
    "Strategic proposal by BroadBrand for Campari Group Africa. Platform rules, regulatory detail, influencer strategy, and 90-day activation roadmap across African markets.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} ${playfair.variable} antialiased grain-overlay`}
      >
        {children}
      </body>
    </html>
  );
}
